/*import {MaybeObservable, observe} from '@tolosa/stream'

export function collection<T>(
	observable: MaybeObservable<T>,
	map: (value: T) => Node
): Node {
	observe(observable, value => {

	})
}
**/
